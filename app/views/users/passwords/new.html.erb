<div class="container">
  <div class="row">
    <div class="col-md-6 offset-md-3">
      <h3 class="mt-3 mb-3">パスワード再設定</h3>
      <p>
        ご登録時のメールアドレスをご記入ください。<br>
        パスワードの再発行のメールをお送りします。
      </p>
      <hr>
      <%= form_for(resource, as: resource_name, url: user_password_path(resource_name), html: { method: :post }) do |f| %>
        <!-- <%= render "users/shared/error_messages", resource: resource %> -->
      
        <div class="form-group">
          <!-- <%= f.label :email %><br /> -->
          <%= f.email_field :email, autofocus: true, autocomplete: "email", placeholder: "メールアドレス", class: "form-control #{"is-invalid" if resource.errors.messages[:email].present?} samazon-login-input" %>
          <% if resource.errors.messages[:email].present? %>
            <span class="samazon-error-message">
              <strong><%= resource.error.full_messages_for(:email).first %></strong>
            </span>
          <% end %>
        </div>
      
        <div class="actions">
          <%= f.submit "送信", class: 'btn samazon-submit-button w-100', style: "margin-top: 10px;" %>
        </div>
      <% end %>
    </div>
  </div>
</div>

<!-- <%= render "users/shared/links" %> -->
